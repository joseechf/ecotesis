
* `geo_helper.dart` (carga y parseo de GeoJSON, polígonos, markers, dialogs).
* `mapazuero.dart` (pantalla principal con `MiniMap`).
* `minimapa.dart` (control de capas, cache, detección de polígonos, hit detection, toggles, etc.).
* Uso de `flutter_map`, `latlong2`, `easy_localization`.
* Assets GeoJSON para terrenos y sembrados.

Listo para lo que quieras hacer ahora.
